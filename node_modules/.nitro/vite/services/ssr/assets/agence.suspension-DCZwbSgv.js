import { t as DashboardShell } from "./DashboardShell-BexnO34z.js";
import { t as ApiError } from "./http-DhyEQgDt.js";
import { t as EmptyState } from "./EmptyState-tYTEkNIz.js";
import { t as ActionModal } from "./ActionModal-bG2-G2VU.js";
import { c as StatusTabs, i as SectionCard, l as TextAreaField, r as SearchInput, s as StatusBadge } from "./Blocks-BPzJNs1k.js";
import { n as StackSkeleton } from "./Skeletons-BmbDCxzK.js";
import { n as ListPagination, t as FilterSelect } from "./ListControls-CdCTDF4c.js";
import { t as DataTable } from "./DataTable-Dz2k6f1R.js";
import { i as respondToSuspension, n as getSuspensionCases, r as getSuspensionHistory } from "./agency-projects.service-G52u-XRU.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { ShieldAlert } from "lucide-react";
//#region src/routes/_authenticated/agence.suspension.tsx?tsr-split=component
/** Suspension (Agence) — litiges, signalements, historique. */
var TABS = [
	{
		value: "all",
		label: "Tous"
	},
	{
		value: "amicable",
		label: "Résolution amiable"
	},
	{
		value: "dispute",
		label: "Litige"
	},
	{
		value: "closed",
		label: "Clôturés"
	}
];
function buildColumns(onSelect) {
	return [
		{
			key: "case",
			header: "Dossier",
			width: "minmax(0,2.2fr)",
			render: (item) => /* @__PURE__ */ jsxs("div", {
				className: "min-w-0",
				children: [/* @__PURE__ */ jsx("p", {
					className: "truncate text-[13.5px] font-bold",
					children: item.projectTitle
				}), /* @__PURE__ */ jsx("p", {
					className: "truncate text-[13px] text-muted-foreground",
					children: item.clientName
				})]
			})
		},
		{
			key: "reason",
			header: "Motif",
			render: (item) => /* @__PURE__ */ jsx("p", {
				className: "truncate text-[13px]",
				children: item.reason
			})
		},
		{
			key: "status",
			header: "Statut",
			render: (item) => /* @__PURE__ */ jsx(StatusBadge, { label: item.statusLabel })
		},
		{
			key: "openedAt",
			header: "Ouvert le",
			render: (item) => /* @__PURE__ */ jsx("p", {
				className: "truncate text-[13px] text-muted-foreground",
				children: item.openedAt
			})
		},
		{
			key: "moderator",
			header: "Modérateur",
			render: (item) => /* @__PURE__ */ jsx("p", {
				className: "truncate text-[13px] text-muted-foreground",
				children: item.moderator ?? "—"
			})
		},
		{
			key: "action",
			header: "Action",
			render: (item) => /* @__PURE__ */ jsx("button", {
				type: "button",
				onClick: () => onSelect(item),
				className: "rounded-md border border-border px-3 py-2 text-[13px] font-semibold transition-colors hover:bg-accent",
				children: "Répondre"
			})
		}
	];
}
function AgencySuspensionPage() {
	const queryClient = useQueryClient();
	const [query, setQuery] = useState("");
	const [activeTab, setActiveTab] = useState("all");
	const [isRespondOpen, setIsRespondOpen] = useState(false);
	const [response, setResponse] = useState("");
	const [selectedCase, setSelectedCase] = useState(null);
	const [page] = useState(1);
	const casesQuery = useQuery({
		queryKey: [
			"agency",
			"suspensions",
			activeTab,
			query,
			page
		],
		queryFn: () => getSuspensionCases({
			...query.trim() ? { query: query.trim() } : {},
			...activeTab !== "all" ? { status: activeTab } : {},
			page,
			pageSize: 20
		})
	});
	const cases = casesQuery.data?.items ?? [];
	const isLoading = casesQuery.isLoading;
	const counts = casesQuery.data?.counts ?? {};
	const totalPages = casesQuery.data?.totalPages ?? null;
	const historyQuery = useQuery({
		queryKey: [
			"agency",
			"suspensions",
			"history",
			selectedCase?.id
		],
		queryFn: () => getSuspensionHistory(selectedCase?.id ?? ""),
		enabled: selectedCase !== null
	});
	const history = historyQuery.data ?? [];
	const isHistoryLoading = historyQuery.isLoading;
	const respondMutation = useMutation({
		mutationFn: (payload) => {
			if (!selectedCase) throw new Error("Sélectionnez d'abord un dossier dans la liste.");
			return respondToSuspension(selectedCase.id, payload);
		},
		onSuccess: () => {
			toast("Réponse envoyée");
			queryClient.invalidateQueries({ queryKey: ["agency", "suspensions"] });
			setIsRespondOpen(false);
			setResponse("");
		},
		onError: (error) => {
			toast(error instanceof ApiError ? error.message : "Endpoint indisponible pour le moment.");
		}
	});
	return /* @__PURE__ */ jsxs(DashboardShell, {
		role: "agency",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "mx-auto max-w-[1080px]",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "grid grid-cols-1 gap-4 sm:grid-cols-[minmax(0,1fr)_auto] sm:items-center",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex min-w-0 items-start gap-3",
						children: [/* @__PURE__ */ jsx(ShieldAlert, {
							className: "mt-1 h-[22px] w-[22px] shrink-0",
							strokeWidth: 1.6
						}), /* @__PURE__ */ jsxs("div", {
							className: "min-w-0",
							children: [/* @__PURE__ */ jsx("h1", {
								className: "text-[24px] font-bold tracking-tight",
								children: "Suspension"
							}), /* @__PURE__ */ jsx("p", {
								className: "mt-1 text-[14px] text-muted-foreground",
								children: "Litiges, signalements et suivi de leur résolution."
							})]
						})]
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: () => {
							if (!selectedCase) {
								toast("Sélectionnez d'abord un dossier dans la liste ci-dessous.");
								return;
							}
							setIsRespondOpen(true);
						},
						className: "rounded-md bg-primary px-4 py-2.5 text-[13.5px] font-semibold text-primary-foreground transition-opacity hover:opacity-90 sm:justify-self-end",
						children: "Répondre à un signalement"
					})]
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-7",
					children: /* @__PURE__ */ jsx(SearchInput, {
						value: query,
						onChange: setQuery,
						placeholder: "Rechercher un dossier..."
					})
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-6",
					children: /* @__PURE__ */ jsx(StatusTabs, {
						tabs: TABS,
						value: activeTab,
						onChange: setActiveTab,
						counts
					})
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-6 grid grid-cols-1 gap-6 sm:grid-cols-3",
					children: [
						/* @__PURE__ */ jsx(FilterSelect, {
							label: "Statut",
							placeholder: "Tous les statuts"
						}),
						/* @__PURE__ */ jsx(FilterSelect, {
							label: "Période",
							placeholder: "Toutes les périodes"
						}),
						/* @__PURE__ */ jsx(FilterSelect, {
							label: "Motif",
							placeholder: "Tous les motifs"
						})
					]
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-6",
					children: /* @__PURE__ */ jsx(DataTable, {
						columns: buildColumns((item) => {
							setSelectedCase(item);
							setIsRespondOpen(true);
						}),
						rows: cases,
						isLoading
					})
				}),
				/* @__PURE__ */ jsx(ListPagination, {
					page,
					totalPages
				}),
				/* @__PURE__ */ jsx("section", {
					className: "mt-9",
					children: /* @__PURE__ */ jsx(SectionCard, {
						title: "Historique des signalements",
						description: "Chronologie des échanges et décisions de modération.",
						children: isHistoryLoading ? /* @__PURE__ */ jsx(StackSkeleton, { count: 3 }) : history.length === 0 ? /* @__PURE__ */ jsx(EmptyState, { message: "Aucune donnée disponible" }) : /* @__PURE__ */ jsx("ul", {
							className: "space-y-4",
							children: history.map((entry) => /* @__PURE__ */ jsxs("li", {
								className: "border-l border-border pl-4",
								children: [
									/* @__PURE__ */ jsx("p", {
										className: "text-[13px] text-muted-foreground",
										children: entry.date
									}),
									/* @__PURE__ */ jsx("p", {
										className: "mt-0.5 text-[13.5px] font-semibold",
										children: entry.title
									}),
									/* @__PURE__ */ jsx("p", {
										className: "text-[13px] text-muted-foreground",
										children: entry.description
									})
								]
							}, entry.id))
						})
					})
				})
			]
		}), /* @__PURE__ */ jsx(ActionModal, {
			open: isRespondOpen,
			onOpenChange: (open) => {
				setIsRespondOpen(open);
				if (!open) setSelectedCase(null);
			},
			title: "Répondre au signalement",
			description: selectedCase ? `Dossier "${selectedCase.projectTitle}" — votre réponse est transmise au client et au modérateur.` : "Votre réponse est transmise au client et au modérateur.",
			confirmLabel: respondMutation.isPending ? "Envoi..." : "Envoyer la réponse",
			onConfirm: () => {
				if (!response.trim()) {
					toast("Renseignez une réponse avant d'envoyer.");
					return;
				}
				respondMutation.mutate({ message: response.trim() });
			},
			children: /* @__PURE__ */ jsx(TextAreaField, {
				label: "Votre réponse",
				rows: 5,
				value: response,
				onChange: (event) => setResponse(event.target.value)
			})
		})]
	});
}
//#endregion
export { AgencySuspensionPage as component };
